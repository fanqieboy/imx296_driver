# --- 1. 核心产物 (保留源码，屏蔽成品) ---
*.ko
*.o
*.mod
*.mod.o
*.mod.c
*.code-workspace

# --- 2. 隐藏的编译指令 (解决你提到的 .ko.cmd, .mod.o.cmd 等) ---
# 这条规则能把所有目录下（包括 src）以 . 开头且以 .cmd 结尾的文件全部干掉
.*.cmd

# --- 3. 符号表与元数据 (src 外部的文件) ---
modules.order
Module.symvers

# --- 4. macOS 专用 (既然你在 Mac 下开发) ---
.DS_Store